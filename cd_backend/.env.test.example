# Test Environment Configuration

# Test mode
NODE_ENV=test

# Test database name (will be created in PostgreSQL)
TEST_DATABASE=cd_backend_test

# Docker PostgreSQL connection (default from docker-compose)
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=postgres

# SSL is disabled for Docker PostgreSQL tests
# This is handled in the code, no environment variable needed

# Minimal JWT configuration for tests
SUPABASE_JWT_SECRET=test_jwt_secret_for_testing_only

# Minimal Bunny Stream mock configuration
BUNNY_STREAM_BASE_URL=https://video.bunnycdn.com
BUNNY_STREAM_API_KEY=test_key
BUNNY_STREAM_LIBRARY_ID=12345
BUNNY_STREAM_PULL_ZONE=test-zone
BUNNY_STREAM_COLLECTION_ID=test-collection 