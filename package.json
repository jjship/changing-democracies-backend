{
  "scripts": {
    "setup": "./scripts/setup-dev.sh",
    "start": "docker compose up",
    "build": "docker compose build",
    "down": "docker compose down --volumes=false",
    "cleanup": "./scripts/cleanup-dev.sh"
  }
}
